{
  "name": "pierrejo-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/pierre-forgejo.js --bundle --format=esm --target=es2022 --outdir=dist --entry-names=[name] --chunk-names=chunks/[name]-[hash] --splitting --minify"
  },
  "dependencies": {
    "@pierre/diffs": "1.3.3",
    "@pierre/trees": "1.0.0-beta.4"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
